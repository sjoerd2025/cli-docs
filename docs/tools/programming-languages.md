# programming-languages

undefined

## go

**Go programming language**

### Commands

| Command | Description |
|---------|-------------|
| `go build` | Build binary |
| `go run <file>` | Run code |
| `go test` | Run tests |
| `go mod init <module>` | Initialize module |
| `go mod tidy` | Tidy dependencies |
| `go get <package>` | Get package |
| `go fmt` | Format code |
| `go vet` | Vet code |
| `go install` | Install binary |
| `go doc <package>` | Show documentation |

[📚 Documentation](https://golang.org/)

---

## node

**JavaScript runtime**

### Commands

| Command | Description |
|---------|-------------|
| `node <file>` | Run JavaScript |
| `node -e '<code>'` | Execute code |
| `node --version` | Show version |
| `node --inspect <file>` | Debug script |
| `node --repl` | Interactive REPL |
| `node --watch <file>` | Watch for changes |
| `node --experimental-modules <file>` | ES modules |
| `node --max-old-space-size=4096 <file>` | Increase memory |
| `node --prof <file>` | Profile code |
| `node -p "expression"` | Print expression |

[📚 Documentation](https://nodejs.org/)

---

## python

**Python programming language**

### Commands

| Command | Description |
|---------|-------------|
| `python script.py` | Run script |
| `python -m <module>` | Run module |
| `python -i <file>` | Interactive mode |
| `python --version` | Show version |
| `python -m pip install <pkg>` | Install package |
| `python -m venv <dir>` | Create virtual env |
| `python -m pytest` | Run tests |
| `python -m pdb <file>` | Debug mode |
| `python -c "code"` | Execute code |
| `python -m py_compile <file>` | Compile code |

[📚 Documentation](https://www.python.org/)

---

